defaults:
  - _self_
  - paths: jz

# Target resolution to which the images will be regridded, in km.
# Give a list [longitude_res, latitude_res]
target_resolution_km: [15.0, 15.0]

# Seed for the train-val-test splitting
splitting_seed: 42
train_val_test_fraction: [0.8, 0.1, 0.1]
