# @package _global_
defaults:
  - override /paths: jz
  - override /sources: precip_interp
  - override /lightning_module: mae
  - override /trainer: 4gpus
  - override /lr_scheduler: cosine_warmup_300epochs.yaml

wandb:
  name: sat_interp_37GHz

dataset:
  train:
    reference_times_interval: 6
    dt_max: 24
    min_available_sources_prop: 0.5

dataloader:
  batch_size: 16
