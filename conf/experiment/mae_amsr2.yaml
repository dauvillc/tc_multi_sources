# @package _global_
defaults:
  - override /paths: jz
  - override /sources: amsr2_5freqs
  - override /lightning_module: mae
  - override /trainer: 4gpus
  - override /lr_scheduler: cosine_warmup_300epochs.yaml
  - override /model: vit_170M

wandb:
  name: amsr2_vit_170M

dataset:
  train:
    reference_times_interval: 6
    dt_max: 24
    min_available_sources_prop: 1.0

dataloader:
  batch_size: 16
