# @package _global_
defaults:
  - override /paths: jz
  - override /sources: amsr2_5freqs
  - override /lightning_module: mae
  - override /lr_scheduler: cosine_warmup_150epochs.yaml
  - override /model: vit_60M

wandb:
  name: amsr2_vit_60M

dataset:
  train:
    dt_max: 1
    min_available_sources: 5
