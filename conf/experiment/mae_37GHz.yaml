# @package _global_
defaults:
  - override /paths: jz
  - override /sources: sat_interp_37GHz
  - override /lightning_module: mae
  - override /lr_scheduler: cosine_warmup_150epochs.yaml

wandb:
  name: sat_interp_37GHz
  group: 37GHz

dataset:
  train:
    dt_max: 6
    min_available_sources: 2 
    single_channel_sources: false
    dist_to_center_as_channel: true
  val:
    single_channel_sources: false

model:
  backbone:
    _target_: multi_sources.models.backbone.MultisourceGeneralBackbone
