# @package _global_
defaults:
  - override /sources: pmw_37_89
  - override /lightning_module: fm_denoiser
  - override /lr_scheduler: cosine_warmup_100epochs
  - override /metrics: default_fm
  - override /model: msvit_12b_d512

lightning_module:
  patch_size: 8
  use_det_model_from_run: null

wandb:
  name: denoiser_pmw_dt1
  group: denoiser_pmw_dt1

dataset:
  train:
    dt_max: 1
    min_available_sources: 2
    source_types_max_avail:
      pmw: 3
  
trainer:
  strategy: ddp
