# @package _global_
defaults:
  - override /paths: local
  - override /lightning_module: mae
  - override /trainer: 1gpu
  - override /sources: amsr2_5freqs

wandb:
  name: test

dataset:
  train:
    reference_times_interval: 3
    dt_max: 24
    min_available_sources_prop: 1.0

dataloader:
  batch_size: 16
