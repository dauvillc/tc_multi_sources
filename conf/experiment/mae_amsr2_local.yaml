# @package _global_
defaults:
  - override /paths: local
  - override /lightning_module: mae
  - override /trainer: 1gpu
  - override /sources: amsr2_5freqs

wandb:
  name: amsr2

dataloader:
  batch_size: 16

lightning_module:
  coords_dim: 512
  pixels_dim: 512
