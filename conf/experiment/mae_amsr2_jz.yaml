# @package _global_
defaults:
  - override /paths: jz
  - override /lightning_module: mae
  - override /trainer: 4xv100-32g

dataloader:
  batch_size: 8

model:
  coordinates_attention:
    layer_class:
      _target_: hydra.utils.get_class
      path: multi_sources.models.attention.CoordinatesAttention
    inner_dim: 512
    num_heads: 8

wandb:
  name: amsr2_cvit_coords_only
