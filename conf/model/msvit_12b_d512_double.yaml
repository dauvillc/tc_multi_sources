dropout: 0.0
values_dim: 512
coords_dim: 256
update_coords: true
backbone:
  _target_: multi_sources.models.motif_double.backbone.MultisourceGeneralBackbone
  values_dim: ${lightning_module.values_dim}
  coords_dim: ${lightning_module.coords_dim}
  n_blocks: 12
  dropout: ${model.dropout}
  att_inner_ratio: 2
  mlp_inner_ratio: 2
  num_heads: 8
  anchor_points_spacing: 4
  window_size: 12
